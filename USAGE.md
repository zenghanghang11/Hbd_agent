# 地址 bad case 筛查

对香港地址列表做质量筛查, 逐条判定是否 bad case, 给出问题类型、严重度和修复建议.

> **说明**: Jimmy 的需求附件未随任务送达(会话内没有该文件), 本版规则是按地址列表本身
> 暴露出的问题类型设计的通用版. 拿到附件后按其口径调整 `RULES` 表即可, 解析层不用动.

## 运行

```bash
python3 src/screen_bad_cases.py --input data/addresses.txt --outdir output
python3 tests/test_screening.py      # 回归测试
```

依赖: Python 3.7+; xlsx 输出需 `pip install openpyxl`(缺失时自动跳过 xlsx, 其余照常输出).

## 产出

| 文件 | 用途 |
|---|---|
| `output/bad_case_screening.xlsx` | 主交付件. 三个表: 明细 / 规则统计 / 数据集级发现, 带筛选与严重度底色 |
| `output/bad_case_screening.csv` | 同明细表, UTF-8-BOM, 便于导入 |
| `output/bad_case_screening.json` | 同明细表, 便于下游程序消费 |
| `output/bad_case_report.md` | 人读版报告: 统计 + P0/P1 清单 + 数据集级发现 + 处理优先级 |

## 判定口径

命中任一「严重」或「一般」规则即计为 bad case; 只命中「轻微」规则的记录可由
`hk_address.normalize()` 自动修复, 不计入.

| 档 | 含义 | 处理方式 |
|---|---|---|
| 严重 P0 | 无法可靠定位到唯一楼宇 | 必须人工修正 |
| 一般 P1 | 影响去重与字段一致性 | 建议修正 |
| 轻微 P2 | 纯格式问题 | 脚本自动修复 |

## 规则表

| 代码 | 类别 | 严重度 | 说明 |
|---|---|---|---|
| C001 | 完整性 | 严重 / 一般* | 缺少大區(九龍/新界/港島). *地區已知时可反查, 降为一般 |
| C002 | 完整性 | 严重 | 缺少地區 |
| C003 | 完整性 | 严重 | 只有座/閣名, 缺屋苑, 无法唯一定位 |
| C004 | 完整性 | 一般 | 缺少街道 |
| C005 | 完整性 | 一般 | 缺少座/期号 |
| A001 | 异常 | 严重 | 尾部粘连表单字段名等噪声(如 `九龍樓層`) |
| A002 | 异常 | 严重 | 疑似形近错别字(如 `康寧間` → `康寧閣`) |
| A003 | 异常 | 一般 | 座号数值偏大, 疑似超出屋苑实际座数 |
| A004 | 异常 | 一般 | 存在无法归类的地址片段 |
| A005 | 异常 | 一般 | 连写地址, 结构解析置信度低 |
| A006 | 异常 | 一般 | 可依据同批数据中的同族记录补全 |
| D001 | 重复 | 一般 | 原文完全重复 |
| D002 | 重复 | 一般 | 归一化后重复(仅格式差异) |
| D003 | 重复 | 一般 | 同一楼宇存在详略两种写法 |
| F001–F007 | 格式 | 轻微 | 全角标点 / 首尾空白 / 座号小写 / 全角字母数字 / 空段 / 无分隔符连写 / 罗马数字期数 |

## 代码结构

- `src/hk_address.py` — 归一化 + 结构化解析(大區/地區/街道/屋苑/期/座), 不含判定逻辑
- `src/screen_bad_cases.py` — 规则引擎 + 报告生成; 规则集中在 `RULES` 字典
- `tests/test_screening.py` — 锁死解析结果与规则命中的回归测试

### 解析要点

- **分段地址**(逗号分隔): 逐段分类, 不依赖固定顺序; 香港常见"細區+大區"并列写法
  (`大圍,沙田`)拆到 `地區` 与 `地區(上级)` 两列.
- **连写地址**(无分隔符): 从尾部逐层剥离 座 → 期 → 楼宇名 → 屋苑 → 地名.
  楼宇名按 2/3/1 字逐个长度试探, 取"剩余部分仍像地址"的切法 —— 否则
  `美城苑貴城閣` 会被贪婪切成 `美城` + `苑貴城閣`. 这类地址统一标记 A005(置信度低).

## 调整方法

- 改判定口径: 编辑 `screen_bad_cases.py` 的 `RULES`(类别/严重度/说明/建议), 无需动解析层.
- 新增地名/屋苑后缀/噪声词: 编辑 `hk_address.py` 顶部的 `DISTRICTS` / `ESTATE_SUFFIX` /
  `NOISE_TOKENS` / `SUSPECT_CHAR_MAP` 词表.
- 座号阈值: `screen_bad_cases.py` 的 `HIGH_BLOCK_THRESHOLD`(默认 20).

## 已知边界

- **A002/A003 是启发式提示, 不是结论**. 脚本没有接入香港屋苑/座数权威数据
  (差饷物业估价署 / 郵政署 lookup), 所以"疑似错别字""座号偏大"都需人工或接口核实,
  脚本不会自动改写任何地名用字.
- **A006 的补全建议来自同批数据的同族记录**(如以 `愉澤閣` 推 `愉和閣` 属 `愉田苑`),
  是推断而非查证, 落库前需确认.
- 接入官方地址库后, 建议把 A002/A003/A006 从"提示"升级为"校验", 并把 P0 规则做成
  入库前的拦截校验.
